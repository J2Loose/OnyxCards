import React from 'react'
import Sidebar from '../Sidebar'
import CreateDeck from '../Pages(P)/Creation'

export default function Create() {

    return (
        <div>
            <Sidebar />
            <CreateDeck />
        </div>
    )
}
