<ModalWrapper>
                            <CardModal2 show={cardOpen}>
                                <CardForm2 onSubmit={handleCreate}>
                                    <strong>Add some cards to this deck!</strong>
                                    <CardLabel>Front</CardLabel>
                                    <CardInput type='text' value={front} onChange={e => setFront(e.target.value)} required/>
                                    <CardLabel>Back</CardLabel>
                                    <CardInput type='text' value={back} onChange={e => setBack(e.target.value)} required/>
                                    <CardLabel>Colour</CardLabel>
                                    <CardSelect value={colour} onChange={e => setColour(e.target.value)} required>
                                        <SelectOption>White</SelectOption>
                                        <SelectOption>Black</SelectOption>
                                        <SelectOption>Red</SelectOption>
                                        <SelectOption>Orange</SelectOption>
                                        <SelectOption>Yellow</SelectOption>
                                        <SelectOption>Green</SelectOption>
                                        <SelectOption>Blue</SelectOption>
                                        <SelectOption>Indigo</SelectOption>
                                        <SelectOption>Violet</SelectOption>
                                        <SelectOption>Pink</SelectOption>
                                    </CardSelect>
                                    <ButtonWrapper>
                                        <Button variant='outline-secondary' onClick={closeCreator}>Close</Button>
                                        <Button variant='outline-success' type='submit'>Add Card</Button>
                                    </ButtonWrapper>
                                </CardForm2>
                            </CardModal2>
                            

            <CardSection onClick={openModal}>
              <AiOutlinePlus />
            <ModalWrapper>
                    <CardModal show={open} onHide={closeModal}>
                        <CardForm onSubmit={handleSubmit} show={open}>
                            <CardLabel>Front</CardLabel>
                            <CardInput type='text' value={front} onChange={e => setFront(e.target.value)} required/>
                            <CardLabel>Back</CardLabel>
                            <CardInput type='text' value={back} onChange={e => setBack(e.target.value)} required/>
                            <CardLabel>Colour</CardLabel>
                            <CardInput type='text' value={colour} onChange={e => setColour(e.target.value)} required />
                            <ButtonWrapper>
                                <Button variant='outline-secondary' onClick={closeModal}>Close</Button>
                                <Button variant='outline-primary' type='submit'>Add Deck</Button>
                            </ButtonWrapper>
                        </CardForm>
                    </CardModal>
                </ModalWrapper>  
            </CardSection>